# Phase 0 audit — KProcess/KThread lock-site classification

Generated by `/tmp/classify_locks.py` on session wrap-up. Raw data at
`/tmp/lock_sites_classified.csv` (regenerate with the script).

## Summary

| Cat | Sites | P (KProcess) | T (KThread) | Description |
|---:|---:|---:|---:|---|
| **A** |  101 |  18 |  83 | already under scheduler lock — direct migration in Phase 3 |
| **B** |  114 |  75 |  39 | **SVC path missing scheduler lock** — Phase 1 target |
| **C** |   29 |   0 |  29 | short-lived read-only temporaries |
| **D** |  174 |  69 | 105 | **HLE / service-thread** — Phase 2 target, hardest category |
| **E** |   24 |  12 |  12 | construction / initialization |
| **F** |  363 | 125 | 238 | test code — use `SyncCell::test_access` |
| **?** |   95 |  32 |  63 | unclassified (mix of false positives + genuine untagged sites) |

Non-test actionable sites: **~537** (categories A–E + manual review of `?`).

The `?` category contains many false positives (e.g., `timer_thread.lock()`,
`on_thread_init.lock()`, `cp.lock()` on content providers) that my regex
conflated with KThread/KProcess because they share variable-name shapes.
Real count after filtering those is likely ~40–60 genuine sites.

## Files holding `Mutex<KThread>` (13)

Production code only, from `grep -l "Mutex<KThread>"`:

- `core.rs`, `cpu_manager.rs`
- `hle/kernel/kernel.rs`, `hle/kernel/k_thread.rs`, `hle/kernel/k_process.rs`
- `hle/kernel/k_scheduler.rs`, `hle/kernel/global_scheduler_context.rs`
- `hle/kernel/k_condition_variable.rs`, `hle/kernel/k_synchronization_object.rs`
- `hle/kernel/k_hardware_timer.rs`, `hle/kernel/physical_core.rs`
- `hle/kernel/svc/svc_thread.rs`
- `hle/kernel/sync_cell.rs` (this refactor's new scaffolding)

## Files holding `ProcessLock` / `Mutex<KProcess>` / `TrackedMutex<KProcess>` (~35+)

Same grep on `ProcessLock\|Mutex<KProcess>\|TrackedMutex<KProcess>`:

- Kernel: `core.rs`, `cpu_manager.rs`, all of `hle/kernel/`
- SVCs: every file in `hle/kernel/svc/`
- **Many HLE service files**: `hle/service/am/*`, `hle/service/filesystem`,
  `hle/service/hid`, `hle/service/ns`, and ~25 more.

This distribution explains why **Category D is the refactor's hot zone**:
HLE services (display layer, applet, process_holder, etc.) hold KProcess
handles on dedicated host threads without any notion of the scheduler
spin-lock.

## Recommended Phase 1 entry point

Start with `hle/kernel/svc/svc_lock.rs` and
`hle/kernel/svc/svc_condition_variable.rs` — the two SVC families
directly implicated in the localized deadlock
(`project_mk8d_arbitrate_unlock_deadlock.md`). If wrapping those with
`KScopedSchedulerLock` at SVC entry closes the deadlock (even without
removing the Mutex), we get immediate MK8D boot progress AND validate
the Phase 3 direction.

If not, iterate through the other svc_* files by volume.

## Tooling produced this session (kept)

- `core/src/hle/kernel/sync_cell.rs` — `SyncCell<T>` + `KProcessCell` /
  `KThreadCell` aliases. Unused. Compiles. Tests pass (the ones that
  don't require the whole crate test harness, which has 136 pre-existing
  unrelated errors).
- `/tmp/classify_locks.py` — audit classifier. Rerun after Phase 1
  lands to track progress.
- `/tmp/lock_sites_classified.csv` — full classification data.

## Session findings: Phase 1/2 are unworkable, plan revised

Four separate tactical fixes to the lock model were attempted this session.
All four regressed boot (details in
`~/.claude/.../memory/project_mk8d_lock_refactor_attempts_all_regressed.md`):

| Change | Direction | Baseline | Result |
|---|---|---:|---:|
| preempt clone-before-yield | remove sync | 53 s | 3 s |
| access_unlocked pilot | switch primitive at one site | 53 s | 23 s |
| handle_interrupt thread→process restructure | remove sync | 53 s | 4 s |
| handle_interrupt add KScopedSchedulerLock (Phase 1a) | **ADD** sync, upstream-faithful | 53 s | **1 s / 0 audio** |

The fourth is the telling one: **adding** scheduler-lock coverage at a
single site (exactly as upstream does it) regressed the hardest. This
proves the incremental Phase 1 / Phase 2 approach cannot work; the
codebase's current equilibrium is too tangled for single-site changes
in either direction.

## Revised plan: upstream translation + environment prerequisites

The audit's "categories A–F" framing was premature procrastination. The
correct framing is: **port upstream directly, but first fix the ruzu
subsystems that upstream's code assumes are true.**

Upstream `SignalToAddress` starts with:
```cpp
KThread* owner_thread = GetCurrentThreadPointer(kernel);  // always valid
```

Ruzu's `get_current_emu_thread()` returns `Option<Arc<Mutex<KThread>>>`
and frequently returns `None` at init / fiber-transition moments. Phase 1a
failed exactly there: `scheduler_lock_ptr` was 0 at the interrupt
moment because the current thread hadn't been populated yet, so the
upstream-faithful code took a silent no-lock fallback and raced.

**Corrected phase ordering:**

1. **Make ruzu's current-thread accessor match upstream's totality** —
   `get_current_emu_thread()` must always return a valid KThread during
   CPU execution (fall back to an idle thread when nothing else is
   scheduled, matching upstream). This is its own session: touches
   thread-local population, fiber-switch transitions, HLE handlers.
2. Translate condvar / arbiter / wait paths directly from upstream —
   now that `scheduler_lock_ptr` is always valid, `KScopedSchedulerLock`
   and `KScopedSchedulerLockAndSleep` ports work as intended.
3. Do the atomic type swap: `Arc<Mutex<KProcess>>` → `Arc<KProcessCell>`,
   `Arc<Mutex<KThread>>` → `Arc<KThreadCell>`. Compiler enforces
   atomicity; every `.lock().unwrap()` call site must be rewritten.
4. Remove `TrackedMutex` and the pre-existing diagnostic scaffolding.

Each step is its own commit, each testable. Step 1 is the prerequisite
that this session discovered was missing — Phase 1a's regression was
rooted there, not in the lock changes themselves.

**Do NOT attempt Phases 1/2 from the original plan** (wrap SVC bodies,
HLE migration). Both proven to regress when applied incrementally.

## Progress — 2026-04-20

### Landed

1. ✅ **Step 1 (current-thread accessor totality)** — `ae42dfc` made
   `get_current_thread_id_fast` / `with_current_thread_fast_mut` total.
2. ✅ **Step 3 (atomic type swaps)** — `d0f45e4` (`ProcessLock =
   SyncCell<KProcess>`) + `3514834` (`KThreadLock = SyncCell<KThread>`).
   Both are "zero call-site changes" — SyncCell exposes API-compatible
   `.lock()` / `.try_lock()` / `.lock_with()` / `.from_value()` shims.
3. ✅ **SIGUSR1 investigation overlay** — `ee5bbd3` squash-merged the
   TrackedMutex-based debugger while preserving the SyncCell direction
   (ProcessLock stays `sync_cell::KProcessCell`; TrackedMutex unused).
4. ✅ **Step 2 — condvar + svc_lock**: `a28cbe4` landed
   `kernel::scheduler_lock()` singleton accessor, ripped out the
   "scheduler_lock_ptr == 0 → silently no-op" fallback across
   `k_condition_variable.rs`, and tightened `wait_for_address` to drop
   the scheduler lock BEFORE the fiber-wait begins (holding it across
   the wait deadlocks EndWait on the signaling thread — discovered by
   direct MK8D boot regression during this step). Subsystem B
   (svc_lock) is naturally covered because `ArbitrateLock` /
   `ArbitrateUnlock` just forward into condvar.

5. ✅ **Step 2 — wait-sync path (`svc_synchronization.rs`)** —
   `2ffd357` made `KSynchronizationObject::wait` open
   `KScopedSchedulerLockAndSleep` via `kernel::scheduler_lock()`
   unconditionally (dropped the `scheduler_lock_ptr == 0 →
   RESULT_INVALID_HANDLE` fallback). Same tactical pattern as condvar.
   `synchronize_preemption_state` cleaned up alongside.
6. ✅ **Step 2 — address arbiter** — `3a7c9cc` replaced the
   `HashMap<u64, parking_lot::Condvar>` host-thread-blocking arbiter
   with upstream's intrusive tree + scheduler-lock + `BeginWait`/
   `EndWait` pattern. `AddressArbiterThreadTree` mirrors
   `ConditionVariableThreadTree`. All 5 public methods (`Signal`,
   `SignalAndIncrementIfEqual`, `SignalAndModify…`, `WaitIfLessThan`,
   `WaitIfEqual`) match upstream's scope. `KProcess::wait_address_arbiter`
   became a static method taking `&Arc<ProcessLock>` + `&Arc<KThreadLock>`
   so the wait path can drop the process lock before the fiber-wait.
7. ✅ **Scheduler-lock host-thread ownership** — `2c117db` fixed the
   `is_locked_by_current_thread()` false-positive fallback
   (`m_lock_count > 0`) that made every un-tagged host thread believe
   it owned the lock when any thread did. Introduced
   `current_sched_thread_id()` which returns the guest tid or a
   unique per-OS-thread synthetic ID allocated from a reserved
   `>= 2^63` range. Both `lock()` and `is_locked_by_current_thread()`
   use this accessor.
8. ✅ **Step 4 — cleanup** — `31d6ad1` removed the `ProcessLockTracker`
   RAII guard + `PROCESS_LOCK_HOLDER_{TID,SITE}` statics, the 6
   remaining call sites, the `tracked_mutex.rs` module, and the
   SIGUSR1 dumper block that read them. 339 lines deleted.

### Still pending

9. ⏳ **Event-signal GSC-Mutex round-trip** — `KEvent::lock_scheduler_for_process`
   and `KReadableEvent::lock_scheduler` still acquire
   `process.global_scheduler_context?.lock()` to fetch the
   scheduler-lock pointer instead of using `kernel::scheduler_lock()`.
   Step 6.5 tried to simplify this and regressed boot (1050 → 864-1264 RU).
   The GSC Mutex was accidentally serializing host threads OUTSIDE
   the scheduler spin-lock, acting as a parking layer that cuts
   spin-lock contention when many host threads signal events
   concurrently. Removing it requires either a fast-path for
   contended host-thread acquires on the spin lock or moving the
   parking behavior into `KAbstractSchedulerLock` itself. Deferred.

### MK8D boot progression

Measured in 90s isolated-dir run (RequestUpdate cycles / VSYNC).
Run-to-run variance is high (~300-2200 RU on post-6.4 commits), so
treat individual numbers as rough indicators.

| Branch tip | RU | VSYNC | Notes |
|---|---:|---:|---|
| pre-refactor (65593b9 main) | ~1377 | — | clock fix baseline, 54 s run |
| step 5b (`3514834`) | 882 | 36 | SyncCell for both Process + Thread; no scheduler lock coverage added yet |
| step 6.0 initial attempt | 0 | 16 | held scheduler lock across fiber-wait → deadlock at boot |
| step 6.1 (`a28cbe4`) | 266 | 23 | condvar fixed; arbiter/sync still missing coverage |
| step 6.2 (`2ffd357`) | 238 | 22 | wait-sync scheduler lock unconditional; flat vs 6.1 |
| step 6.3 (`3a7c9cc`) | 1050 | 40 | arbiter full rewrite — recovered above 5b baseline |
| step 6.4 (`2c117db`) | 2202 | 68 | host-thread ID fix in KAbstractSchedulerLock |
| step 6.5 attempt | 864-1264 | 37-47 | event-signal GSC-mutex removal regressed; reverted |
| step 6.6 (`31d6ad1`) | 316-1578 | 24-47 | cleanup only (dead code removal); variance overlap with 6.4 |

Refactor's intermediate-breakage contract was kept: every
intermediate commit either improves throughput or reveals a hidden
bug (like the 6.0 deadlock and the 6.4 ownership false-positive).
