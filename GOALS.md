# Active Goal

Créer une série de branches de travail incrémentales pour faire avancer MK8D avec une architecture saine et une meilleure parité
  upstream.

  Chaque tâche doit être faite sur une branche différente, et chaque branche doit repartir de la branche précédente, pas de main. Ne merge
  dans main que lorsque je le demande explicitement.

  Ordre des branches/tâches:

  1. Branche `fix-mk8d-nvmap-device-memory`
     Objectif: rapprocher `NvMap`, `HeapMapper`, `Container`, `MaxwellDeviceMemoryManager`, SMMU/GMMU et DeviceMemoryManager de upstream.
     Priorité: corriger ou réduire le blocage MK8D observé sur `nvmap::IocAlloc handle=0xAD0 addr=0x068B0000 size=0x70000
     result=0x0000D401`, suivi de `BreakLoopNullPc`.
     Vérifier chaque item DIFF.md lié à nvmap/host1x/device memory contre upstream avant modification.

  2. Branche `fix-mk8d-page-table-kernel-memory`
     À créer depuis `fix-mk8d-nvmap-device-memory`.
     Objectif: rapprocher `KPageTableBase`, `KProcessPageTable`, IPC memory setup, page groups, partial IPC pages, permissions, rollback et
     RAII helpers upstream.
     Priorité: supprimer les mappings stopgap et rendre les chemins `Map/Unmap/LockForDeviceAddressSpace/IPC` plus fidèles à upstream.

  3. Branche `fix-mk8d-ipc-host-thread`
     À créer depuis `fix-mk8d-page-table-kernel-memory`.
     Objectif: rapprocher le lifecycle IPC upstream: `KClientSession::SendSyncRequest`, `KServerSession::OnRequest`,
     `ServerManager::LoopProcess`, wait/reply, scheduler locks.
     Priorité: remplacer progressivement le fallback inline HLE par le chemin host-thread/server-manager upstream-safe, sans régression
     MK8D.

  4. Branche `fix-mk8d-texture-framebuffer`
     À créer depuis `fix-mk8d-ipc-host-thread`.
     Objectif: rapprocher `TextureCache`, `OpenGLTextureCache`, framebuffer lifecycle, render target binding, `PrepareImage`,
     `RefreshContents`, `SynchronizeAliases`, rescale, alias copies, Fermi2D blit et `UpdateRenderTargets`.
     Priorité: réduire les écrans noirs/cinématiques noires/artifacts de rendu MK8D.

  5. Branche `fix-mk8d-shader-environment`
     À créer depuis `fix-mk8d-texture-framebuffer`.
     Objectif: rapprocher `ShaderCache`, `ShaderEnvironment`, `GraphicsEnvironment`, `ComputeEnvironment`, GLSL pipeline construction, live
     owner graph `Maxwell3D/KeplerCompute/MemoryManager/DrawManager`.
     Priorité: supprimer les snapshots/callback transports réduits et se rapprocher du chemin upstream de compilation pipeline.

  Règles générales:
  - Toujours lire upstream dans `/home/vricosti/Dev/emulators/zuyu/src/` avant de modifier.
  - Respecter AGENTS.md: parité de structure, ownership, constantes, ordre et comportement.
  - Pour chaque item DIFF.md traité: vérifier s’il est encore vrai, corriger si nécessaire, puis supprimer l’item de DIFF.md. Si l’item est
  stale, le supprimer. Ne pas ajouter d’entrée “fixed”.
  - Ne pas masquer les bugs par des clamps ou fallbacks non-upstream sans justification explicite.
  - Après chaque branche: `cargo check` ciblé au minimum, puis test MK8D si le changement peut affecter runtime.
  - Avant de considérer une branche terminée: fournir les fichiers modifiés, items DIFF.md supprimés, tests effectués, résultat MK8D,
  risques restants.

## Current branch evidence: fix-mk8d-nvmap-device-memory

- Branch: `fix-mk8d-nvmap-device-memory`.
- Current MK8D nvmap narrowing: the late blocker remains `nvmap::IocAlloc(handle=0xAD0/0xAD4, addr=0x068B0000, size=0x70000) -> 0x0000D401`, followed by `BreakLoopNullPc`.
- Latest device-address-space SVC cleanup: after re-reading upstream `svc_device_address_space.cpp` and `k_device_address_space.cpp`, the three Rust SVC map/unmap paths now share one owner-local resolver for `KDeviceAddressSpace` plus validated `process_handle`. The remaining gap is unchanged and explicit: valid non-current process handles still resolve to the current application process because the Rust `KHandleTable` cannot yet return typed `KProcess` owners.
- Current Host1x/DeviceMemoryManager progress: `Container::open_session` now registers the process `Memory` owner with Host1x SMMU, `MaxwellDeviceMemoryManager` stores that owner per ASID, and `UpdatePagesCachedCount` uses the registered owner for ASID-backed `Memory::rasterizer_mark_region_cached(...)` calls. This removes the earlier `opaque process identity` ASID debt, but still needs runtime validation.
- Current GPU `MemoryManager` progress: `MemoryManager` and its inner `GpuMemoryManager` now store a non-optional Host1x `MaxwellDeviceMemoryManager` owner at construction time, and owner-backed GPU read/write helpers treat that owner as authoritative instead of falling back to legacy CPU guest-memory callbacks when an SMMU page is unmapped. This moves ruzu closer to upstream `MemoryManager(Core::System&, MaxwellDeviceMemoryManager&, ...)`; the old `MemoryManager` guest-memory callback fields and setters have been removed entirely, runtime memory-manager allocation now requires the Host1x owner instead of fabricating an ownerless fallback, and reduced tests install real `MaxwellDeviceMemoryManager` SMMU backing when they need owned reads/writes.
- Current `MemoryManager` callback cleanup: runtime owner-backed `Gpu::allocate_memory_manager_handle` and `ChannelState::init` no longer install redundant `MemoryManager` guest-memory reader/writer callbacks, and production `MemoryManager` builds no longer contain those fallback fields. `Maxwell3D` read paths, `ProcessCBMultiData`, report-semaphore fallback writes, and inline upload paths now prefer the stored `MemoryManager` / `MaxwellDeviceMemoryManager` owner, with callback/pending fallbacks only for ownerless reduced fixtures.
- Current `WriteBlock` parity progress: after re-reading upstream `video_core/memory_manager.cpp::WriteBlockImpl`, Rust `MemoryManager::write_block` now invalidates mapped device segments through the bound `RasterizerInterface` before writing, while `write_block_unsafe` and `write_block_cached` keep the upstream no-invalidation path. Focused test: `cargo test -p video_core write_block_invalidates_but_write_block_unsafe_does_not -- --nocapture`.
- Current typed device-memory progress: `MaxwellDeviceMemoryManager::{read_u*,write_u*,get_pointer*}` now resolves through an upstream-shaped dense device-page `compressed_physical_ptr` table instead of returning stubbed zero/no-op results. This matches upstream typed `DeviceMemoryManager<Traits>::Read/Write/GetPointer` behavior for mapped device pages and directly affects query-cache typed writes.
- Current device-interface progress: Host1x device memory now binds both upstream `DeviceInterface` effects to the rasterizer. Safe SMMU `ReadBlock` calls flush via `RasterizerInterface::flush_region`, SMMU `WriteBlock` calls invalidate via `invalidate_region`, and `smmu_read_block_unsafe` remains available for explicit upstream `ReadBlockUnsafe` users.
- Current VIC write-back audit: after re-reading upstream `video_core/host1x/vic.cpp`, `video_core/guest_memory.h`, and `core/guest_memory.h`, the VIC `GpuGuestMemoryScoped<SafeWrite>` gap was narrowed to a scoped-span/performance adaptation for the currently ported formats. Upstream `SafeWrite` writes back through `MemoryManager::WriteBlock` or invalidates a direct span; ruzu produces scratch output and calls `MaxwellDeviceMemoryManager::smmu_write_block`, which now carries the same Host1x SMMU write and rasterizer invalidation side effect. Remaining VIC debt is SIMD and broader format coverage, not this device-memory write-back item.
- Current SMMU `WalkBlock` progress: `MaxwellDeviceMemoryManager::smmu_read_block(_unsafe)` now continues over unmapped device segments by logging and zero-filling the destination, and `smmu_write_block` logs/skips unmapped segments while still invalidating the full written range. This matches upstream `DeviceMemoryManager<Traits>::ReadBlock` / `WriteBlock` observable behavior more closely; the Rust bool return remains only as a compatibility signal for legacy callers that are not yet owner-authoritative.
- Current reverse-mapping progress: Host1x SMMU now has dense counterparts for upstream `compressed_physical_ptr`, `continuity_tracker`, `cpu_backing_address`, and `compressed_device_addr`, plus `MultiAddressContainer` alias collapse on unmap. Runtime mappings and reduced tests now derive compressed physical pages from registered/test-installed `DeviceMemory` `physical_base`, matching upstream `GetRawPhysicalAddr(ptr) >> page_bits + 1`; no synthetic SMMU physical-page fallback remains.
- Current NvMap fallback progress: non-preallocated `NvMap::pin_handle` now uses the active session ASID and guest CPU backing via `Host1xCoreInterface::smmu_map(...)`, matching upstream's `MaxwellDeviceMemoryManager::Map(address, vaddress, size, session->asid, true)` path for normal sessions. It forwards every covered guest page, including pages whose `Memory::get_pointer_silent(...)` returns null, so the SMMU owner records upstream-style `compressed_physical_ptr = 0` entries instead of skipping the page or falling back to a CPU-address pin. Ownerless `NvMap::new()` is now `#[cfg(test)]`, the public ownerless `new_with_system(...)` constructor was removed, and production `NvMap` construction must go through `Container::new_with_system(...)` with a `ContainerSessionStore`. Container-backed runtime sessions now treat a missing session store, ASID, or process `Memory` owner as an error and free the SMMU range instead of silently falling back to direct host-pointer mapping.
- Current null-backing map parity progress: `HeapMapper` now forwards pages whose `Memory::get_pointer_silent(...)` returns null to Host1x instead of skipping them, and `MaxwellDeviceMemoryManager::smmu_map_internal` clears only the compressed physical entry without invalidating or cleaning reverse/CPU-backing tables, matching upstream `DeviceMemoryManager::Map`'s `compressed_physical_ptr[page] = 0; continue` branch. Focused test: `cargo test -p video_core smmu_map_null_backing_clears_only_compressed_physical -- --nocapture`.
- Current unmap parity progress: `MaxwellDeviceMemoryManager::smmu_unmap` now preserves `continuity_tracker` entries after clearing compressed physical mappings, reverse mappings, and CPU backing, matching upstream `DeviceMemoryManager::Unmap`, which does not touch `continuity_tracker`. Focused test: `cargo test -p video_core smmu_map_with_cpu_backing_tracks_backing_and_continuity -- --nocapture`.
- Current dense table progress: `MaxwellDeviceMemoryManager` now initializes `compressed_physical_ptr`, `continuity_tracker`, and `cpu_backing_address` as dense device-page tables with upstream defaults (`0`, `1`, and `0`), and `compressed_device_addr` as a dense physical-page table initialized to `0`. `UpdatePagesCachedCount` now skips pages whose decoded CPU backing virtual page is zero, matching upstream. The remaining table gap is structural owner shape/test-fixture fallbacks, not compressed-table source.
- Current cache-counter / fastmem protection progress: `MaxwellDeviceMemoryManager::UpdatePagesCachedCount` now uses the existing Rust port of upstream `Common::RangeMutex` / `ScopedRangeLock` instead of a whole-table mutex, and `Memory::rasterizer_mark_region_cached` now mirrors upstream's fastmem `Protect(vaddr, size, perm)` call with `use_reactive_flushing` policy before page-type transitions. The older DIFF.md items for coarse cache-counter locking and missing `RasterizerMarkRegionCached` fastmem `Protect()` parity were removed after re-reading upstream.
- Latest DIFF.md cleanup: after re-reading upstream `core/device_memory_manager.inc` and current `gpu_device_memory_manager.rs`, the older Host1x renderer-binding entry no longer claims compressed physical/device tables are missing. The remaining item there is narrowed to Rust owner-graph adapters around `DeviceMemoryManager` / Host1x / rasterizer lifetimes.
- Latest GMMU bridge audit cleanup: after re-reading upstream `nvhost_as_gpu.cpp` and current `GpuMemoryManagerHandle`, the older `nvhost_as_gpu` DIFF entry no longer claims sparse-vs-reserved mapping or rasterizer invalidation are missing behind the bridge. `Map`, `MapSparse`, `Unmap`, `BindChannel`, and `GPU::InitAddressSpace(...)` now forward to the current video_core `MemoryManager`; remaining work stays tracked in the dedicated `memory_manager.rs` and Host1x/device-memory entries.
- Current ASID-backed physical strictness: `smmu_map_with_cpu_backing` no longer falls back to synthetic physical IDs when host pointers cannot be translated through the registered `physical_base`; it records unmapped compressed physical entries and no CPU backing/reverse mapping, matching upstream `DeviceMemoryManager::Map`'s null-pointer branch. The core `Host1xCoreInterface::smmu_map` direct bridge was removed, and reduced tests now use ASID-backed `smmu_map_with_cpu_backing(...)` with test-installed `physical_base`; no synthetic SMMU physical fallback remains. Focused tests: `cargo test -p video_core smmu_map_with_cpu_backing_requires_physical_base -- --nocapture` and `cargo test -p video_core gpu_device_memory_manager -- --nocapture`.
- Latest Host1x cache-callback cleanup: after re-reading upstream `DeviceMemoryManager<Traits>::UpdatePagesCachedCount`, runtime no longer installs a frontend `mark_region_caching` callback in `ruzu_cmd`; the callback is `#[cfg(test)]` only for reduced video_core fixtures. Runtime pages with a registered ASID owner call that `Memory` owner, and pages without one no-op like upstream's `memory_device_inter == nullptr` guard.
- Latest Host1x cache-callback smoke: `/tmp/mk8d_no_runtime_mark_callback_1780815225.log` reached `am::NotifyRunning` under `RUST_LOG=warn` and timed out cleanly at 120s with no `IocAlloc`, `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `glTextureSubImage3D`, missing runtime SMMU, missing registered SMMU ASID, runtime SMMU map missing, unregistered ASID, `OpenAudioRenderer`, `IAudioRenderer::Start`, or `MapSharedMemory` marker. No `ruzu-cmd` / `yuzu-cmd` process remained after the run.
- Current FlatAllocator parity cleanup: after re-reading upstream `common/address_space.h/inc`, `FlatAllocator` and `FlatAllocator64` now share one generic bool-backed `FlatAllocatorBool<VaType>` core instead of maintaining separate u32/u64 copies. This matches the upstream `FlatAllocator<VaType, UnmappedVa, AddressSpaceBits>` shape for the allocator instantiations used by Host1x SMMU/GMMU. Checks: `cargo check -p common`, `cargo test -p common flat_allocator -- --nocapture`, `cargo check -p video_core`.
- Latest no-synthetic-SMMU smoke: `/tmp/mk8d_no_synthetic_smmu_1780785694.log` reached `am::NotifyRunning` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, `MapSharedMemory`, missing Container session store, missing registered SMMU ASID, or missing process Memory owner marker before controlled timeout. No `ruzu-cmd` process remained after the run.
- Latest no-runtime-`MemoryManager`-callback smoke: `/tmp/mk8d_no_mm_callbacks_1780786160.log` reached `am::NotifyRunning` under `RUST_LOG=warn` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, `MapSharedMemory`, missing Container session store, missing registered SMMU ASID, or missing process Memory owner marker before controlled timeout. No `ruzu-cmd` process was active before the run or left after it. This remains sparse-log no-obvious-regression evidence only; it does not prove audio/rendering progress.
- Latest `MemoryManager` fallback cleanup: after re-reading upstream `video_core/memory_manager.{h,cpp}` and upstream query-cache writeback through `device_memory.WriteBlockUnsafe(...)`, the remaining `MemoryManager` guest-memory callback fields/setters were removed. Fermi2D, SW blitter, OpenGL query-cache, Vulkan query-cache, and OpenGL rasterizer tests that used those setters now install `MaxwellDeviceMemoryManager` SMMU backing and assert final device-memory bytes. Checks: `cargo check -p video_core`, `cargo test -p video_core memory_manager -- --nocapture`, `cargo test -p video_core blit_writes_pitch_linear_surface_through_memory_manager -- --nocapture`, `cargo test -p video_core test_blit_copies_pixels -- --nocapture`, and `cargo test -p video_core test_blit_different_pitches -- --nocapture`.
- Latest `MemoryManager` owner-shape cleanup: after re-reading upstream `video_core/memory_manager.{h,cpp}`, the upstream-shaped public `MemoryManager::{read_block,read_block_unsafe,write_block,write_block_unsafe,write_block_cached,copy_block}` names now use the stored `MaxwellDeviceMemoryManager` owner directly, and the temporary `*_owned` aliases have been removed. `GpuContext::flush` now reads GPFIFO pushbuffers through its owner-backed `GpuMemoryManager::read_block(...)` instead of accepting a CPU guest-memory reader callback, and the explicit `*_with_callback` walkers are test-only reduced fixtures. Checks: `cargo check -p video_core`, `cargo check -p video_core --tests`, `cargo test -p video_core memory_manager -- --nocapture`, `cargo test -p video_core gpu_context -- --nocapture`, `cargo test -p video_core smmu_apply_op_on_host_pointer -- --nocapture`, `cargo fmt --check`, and `git diff --check`.
- Latest `Maxwell3D` / upload owner-path cleanup: `make_gpu_memory_reader`, `read_gpu_block`, and macro parameter refresh now use `MemoryManager::read_block(...)`; `ProcessCBMultiData` uses `MemoryManager::write_block_cached(...)`; report-semaphore fallback writes use `MemoryManager::write_block_unsafe(...)`; and `engine_upload::State` now stores its `MemoryManager` owner plus bound rasterizer handle directly. KeplerMemory and KeplerCompute construct upload state with their `MemoryManager` owner, and runtime `Maxwell3D` construction now uses `Maxwell3D::new_with_memory_manager(...)` from `ChannelState::init` and legacy `GpuContext::new`, matching upstream's constructor-time `MemoryManager&` owner direction more closely. `engine_upload::State::new()`, `Default for State`, `State::bind_memory_manager(...)`, `Maxwell3D::new()`, and `Default for Maxwell3D` are test-only, so normal runtime parent engines cannot introduce ownerless upload state through the public API. `engine_upload::State` no longer stores a copied upload-register snapshot; parent engines pass the current upload `Registers` view into `process_exec` / `process_data_*`, preserving live-register behavior without a self-referential Rust `Registers&`. Remaining gap: the per-call register-view adaptation is still not the literal upstream constructor-stored `Registers&` shape. Focused tests: `cargo test -p video_core read_gpu_block_uses_memory_manager_owner_without_guest_callback -- --nocapture`, `cargo test -p video_core write_block_cached_uses_device_memory_owner_and_accumulates -- --nocapture`, `cargo test -p video_core process_cb_multi_data_uses_memory_manager_owner_without_guest_writer -- --nocapture`, `cargo test -p video_core report_semaphore_fallback_uses_memory_manager_owner_without_guest_writer -- --nocapture`, `cargo test -p video_core inline_upload_linear_uses_constructor_memory_manager_owner_without_guest_writer -- --nocapture`, and `cargo test -p video_core data_register_writes_through_memory_manager -- --nocapture`.
- Latest `Maxwell3D` DIFF audit cleanup: after re-reading upstream `maxwell_3d.{h,cpp}` and `engine_upload.{h,cpp}`, the older DIFF item claiming runtime `Maxwell3D` still binds the upload memory owner only after construction was stale. Current runtime parent-engine construction uses `Maxwell3D::new_with_memory_manager(...)`; the remaining reduced ownerless `Maxwell3D`, `engine_upload::State`, and late owner-injection helpers are now test-only, while shader-owner fixture debt remains tracked by later shader-environment entries.
- Current `KeplerCompute` upload parity progress: after re-reading upstream `kepler_compute.h/cpp`, Rust `KeplerCompute` now owns `engine_upload::State`, `upload_address`, and `uploads`, forwards `BindRasterizer` into upload state, processes `exec_upload` / `data_upload` through upstream-shaped `Upload::State`, detects indirect compute with `MemoryManager::is_memory_dirty(...)`, clears `uploads`, dispatches the bound rasterizer, and resets `indirect_compute` after launch. Remaining gap: launch still drains through Rust `execute_pending(...)` and reduced `QueueMetaData` rather than a literal upstream `ProcessLaunch()` method over raw `LaunchParams`. Checks: `cargo check -p video_core --tests`, `cargo test -p video_core kepler_compute -- --nocapture`.
- Current NvMap allocation-pressure progress: `NvMap::pin_handle` now retries failed SMMU allocation by draining `unmap_queue` one handle at a time through the same owner-local unmap path used by forced free, matching upstream's `PinHandle` allocation retry structure.
- Current NvMap no-fake-device-address progress: reduced no-Host1x `NvMap::new()` fixtures no longer fabricate `d_address = CPU address` when `pin_handle` cannot reach upstream's mandatory Host1x memory manager. The path now returns `0` and leaves `d_address`/`pins` unchanged, matching upstream's no-success-without-Host1x model. `cargo check -p core --tests` now passes after the core test-harness cleanup, so focused core tests are no longer blocked at compile time.
- Current Container ASID fallback cleanup: after re-reading upstream `container.h/cpp`, reduced no-Host1x `Container::new()` sessions now store an explicit invalid ASID instead of silently using ASID 0, and `ContainerSessionStore::session_asid(...)` does not expose it to `NvMap::pin_handle`. Runtime `Container::new_with_system(...)` still registers the real process `Memory` owner through Host1x like upstream.
- Current Host1x-local GMMU progress: `Host1x::new` now builds the Host1x-local `MemoryManager` with the same shared `MaxwellDeviceMemoryManager` owner returned by `Host1x::MemoryManager()`, matching upstream `Host1x::Host1x(...): memory_manager(system.DeviceMemory()), gmmu_manager{system, memory_manager, 32, 0, 12}` instead of creating an ownerless local GMMU.
- Current Host1x renderer-binding DIFF cleanup: after re-reading upstream `GPU::Impl::BindRenderer` and `DeviceMemoryManager::BindInterface`, the remaining callback/bridge around `MaxwellDeviceMemoryManager` invalidation is tracked as the Rust representation of upstream's `DeviceInterface*` edge, not as a separate behavioral `Unintentional differences` item for the audited renderer-binding slice.
- Latest MK8D smoke on this branch: `/tmp/mk8d_owner_upload_smoke_1780795016.log` (`RUST_LOG=warn`, isolated XDG dirs, no L+R input, 150s timeout) reached `am::NotifyRunning` and logged no `nvmap::IocAlloc`, `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, GL API marker, `glTextureSubImage3D`, missing runtime SMMU owner marker, or stale process marker before the controlled timeout. This remains weak no-obvious-regression evidence only; it does not prove the late MK8D blocker fixed.
- Branch handoff status: no concrete remaining `DIFF.md` item was found in the strict `NvMap::PinHandle` / Host1x SMMU / `DeviceMemoryManager` local slice after the latest audit; remaining high-value work is either global owner-graph cleanup or the next requested branch, `fix-mk8d-page-table-kernel-memory`. Current checks for the handoff point: `cargo check -p common`, `cargo check -p core`, `cargo check -p video_core --tests`, `cargo build --release --bin ruzu-cmd`, `cargo fmt --check`, and `git diff --check`. The branch is still uncommitted, so do not create the next branch until this work is explicitly committed or otherwise frozen.
- Latest core test-harness cleanup: test-only imports for `KThreadLock`, `ProcessLock`, `KPort`, `Event`, `IoctlUnmapBuffer`, `IoctlFreeSpace`, and the `RESULT_SUCCESS` owner were aligned with the current Rust modules. Stale `CoreTiming` tests now use `Arc<CoreTiming>::initialize(...)`, old `KProcess::read_block/write_block` tests compile through a `#[cfg(test)]` bridge to `read_memory_vec` / `write_memory`, and stale `KServerSession` process/thread/timer tests now use `ProcessLock`, `KThreadLock`, and `Arc<CoreTiming>`. Remaining stale service tests in `ipc_helpers`, `am`, `audio`, `mii`, and `nvhost_ctrl_gpu` were migrated to current public APIs, and the last native-test borrow-check issues were repaired. Checks: `cargo check -p core`, `cargo check -p core --tests`, `cargo test -p core open_first_open_close_split_across_impl_managers -- --nocapture`, `cargo fmt --check`, `git diff --check`.
- Current `PTEKind` parity progress: `video_core::PteKind` is now a transparent raw-byte newtype instead of a gapped Rust enum, and `MemoryManager::Map` preserves `kind_raw as u8` in `kind_map`. This matches upstream `static_cast<Tegra::PTEKind>(params.kind)` plus `RangeMap<GPUVAddr, PTEKind>` behavior and stops unknown page-kind values from collapsing to `INVALID`.
- Latest `MemoryOperation` sparse/big-page audit cleanup: after re-reading upstream `video_core/memory_manager.h/cpp`, the older DIFF item asking to audit `MemoryOperation` and sparse-vs-big-page handling was stale. Current Rust already carries upstream-shaped big/small walkers, `MapSparse`, sparse `Reserved` zero-fill/skip behavior, big-page fallback into small pages, `IsFullyMappedRange`, `MaxContinuousRange`, and `GetSubmappedRange`; the remaining `MemoryManager` work is owner/storage shape, not this sparse/big-page policy. Checks: `cargo test -p video_core memory_manager -- --nocapture`, `cargo check -p video_core`.
- Latest `gpu_device_memory_manager` stale-stub cleanup: after re-reading upstream `video_core/host1x/gpu_device_memory_manager.h/cpp` and `core/device_memory_manager.inc`, the old DIFF claim that typed reads/writes, pointer lookup, and `UpdatePagesCachedCount` were behavioral stubs was stale. Current Rust has upstream-shaped typed accessors, pointer/span lookup, compressed physical/CPU-backing tables, and cached-page marking; the remaining gap is literal C++ template/owner graph shape. Check: `cargo test -p video_core gpu_device_memory_manager -- --nocapture`.
- Latest runtime after ASID -> `Memory` owner routing: `/tmp/mk8d_registered_memory_asid_targeted_1780769575.log` reached `OpenAudioRenderer` and `am::NotifyRunning` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `glTextureSubImage3D`, or GL API error before timeout. `/tmp/mk8d_registered_memory_asid_profile_1780769802.log` showed `UpdatePagesCachedCount`, `Memory::rasterizer_mark_region_cached`, and fallback mark-region callback profiles all exiting, so this slice does not show an obvious cache-path lock inversion.
- Latest runtime after storing the Host1x owner directly in `MemoryManager`: `/tmp/mk8d_memory_manager_owner_1780770150.log` again reached `OpenAudioRenderer` and `am::NotifyRunning`, logged no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `glTextureSubImage3D`, or GL API error before timeout. Follow-up `/tmp/mk8d_ac8_trace_1780770390.log` with `RUZU_TRACE_NVMAP_LOOP=1` and SIGUSR2 profiles did not reproduce a hard `AC8` block: it reached `nvmap::IocAlloc(handle=0xAC4)` and still reported active GPU/NVN progress (`SubmitList` push/pop/done `3693/3692/3691`, `BQP QueueBuffer=1844`, `Present count=1843`). The earlier `AC8` line is therefore only the last targeted nvmap log in that run, not proof of a hard stall.
- Latest runtime after typed device-memory reads/writes and Host1x flush binding: `/tmp/mk8d_device_memory_clean_1780771304.log` reached `OpenAudioRenderer`, `IAudioRenderer::Start`, and `am::NotifyRunning` within a 90s controlled timeout, with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, or `glTextureSubImage3D` marker. Earlier per-nvmap-info runs were too noisy and are not used as progression evidence.
- Latest runtime after upstream-shaped reverse-map encoding: `/tmp/mk8d_multiaddr_sigusr1_1780771895.log` reached `OpenAudioRenderer`, `IAudioRenderer::Start`, and `am::NotifyRunning`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, or `glTextureSubImage3D` marker was logged before controlled termination. `/tmp/mk8d_multiaddr_1780771778.log` reached audio start but not `NotifyRunning`, so this remains smoke evidence only and does not prove the known intermittent early stall fixed.
- Latest runtime after splitting sparse `compressed_physical_ptr` from host backing: `/tmp/mk8d_compressed_physical_1780772415.log` reached `OpenAudioRenderer` and `IAudioRenderer::Start`, but not `am::NotifyRunning`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, or `glTextureSubImage3D` marker was logged before controlled termination. Treat this as weak no-fatal smoke evidence only.
- Latest runtime after routing non-preallocated `NvMap::pin_handle` through ASID-backed CPU backing: `/tmp/mk8d_nvmap_asid_pin_1780772887.log` reached `am::NotifyRunning` under `RUST_LOG=warn`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, or `glTextureSubImage3D` marker was logged. Because the smoke used warn-level logging, it does not prove audio start.
- Latest runtime after wiring the Host1x-local GMMU to the shared device-memory owner: first 90s smoke `/tmp/mk8d_host1x_gmmu_owner_1780773494.log` reproduced the known early sparse-log stall before `NotifyRunning`; retry `/tmp/mk8d_host1x_gmmu_owner_retry_1780773607.log` reached `am::NotifyRunning` under `RUST_LOG=warn` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, or `glTextureSubImage3D` marker. Treat this as no-obvious-regression smoke only; it does not prove audio start or remove the intermittent early-stall risk.
- Latest runtime after making owner-backed `MemoryManager` reads/writes authoritative: first 90s smoke `/tmp/mk8d_device_memory_no_fallback_1780774311.log` reproduced the known early sparse-log stall before `NotifyRunning`; retry `/tmp/mk8d_device_memory_no_fallback_retry_1780774416.log` reached `am::NotifyRunning` under `RUST_LOG=warn` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, or `glTextureSubImage3D` marker. The retry also logged `svc::MapSharedMemory: address 0xbaa04000 size 0x40000 cannot contain Shared`, reinforcing that the next high-value branch is page-table/kernel-memory parity rather than more NvMap-local work.
- Latest runtime after upstream-exact `Unmap` continuity preservation and null-backing `Map` compressed-physical-only clearing: `/tmp/mk8d_smmu_unmap_nullmap_1780778327.log` reached `am::NotifyRunning` under `RUST_LOG=warn` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, or `glTextureSubImage3D` marker. This is no-obvious-regression smoke only; warn-level logging does not prove audio start or rendering progress.
- Latest runtime after forwarding null guest pages through non-preallocated `NvMap::pin_handle`: `/tmp/mk8d_nvmap_forward_null_pages_1780778824.log` reached `am::NotifyRunning` under `RUST_LOG=warn`, then timed out at 120s with only one later `WriteBuffer: skip empty buffer write service=IStorage cmd=0` warning. No `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, or `glTextureSubImage3D` marker was logged. This is weak no-obvious-regression smoke only and preserves the known intermittent early/post-NotifyRunning stall risk.
- Latest runtime after safe `MemoryManager::WriteBlock` invalidation parity: `/tmp/mk8d_writeblock_invalidate_1780784290.log` reached `am::NotifyRunning` under `RUST_LOG=warn` and timed out at 120s with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, missing Container session store, missing registered SMMU ASID, or missing process Memory owner marker. This is no-obvious-regression smoke only; warn-level logging does not prove audio start or rendering progress.
- Latest page-table retry classification: `/tmp/mk8d_npdm_regions_1780774652.log` confirms MK8D is `Is32BitNoMap`. In that mode, the first shared-memory candidate `0xB9004000..0xBA104000` is inside the folded heap `[0x40000000..0xC0000000)` and should be rejected by upstream `CanContain(Shared)`; the guest retry at `0xD5C04000` succeeds and the run reaches `OpenAudioRenderer` plus `am::NotifyRunning`. This removes `MapSharedMemory`'s first rejection as a primary lead.
- Current memory-history coverage progress: `RUZU_TRACE_MEM_HISTORY_TARGET=0x068B0000` now also records `KServerSession::SetupForIpc` map-alias events, closing the prior blind spot where IPC could materialize pages without `MapMemory`/`MapPhysicalMemory` SVC records. `/tmp/mk8d_memhist_ipc_map_alias_1780774959.log` did not reproduce the late `0x068B0000` failure, so this instrumentation is ready but has not yet classified the blocker.
- Latest restart after IPC map-alias memory-history coverage: `/tmp/mk8d_memhist_retry_1780775510.log` reached `am::NotifyRunning` and timed out at 240s with no `0x068B0000`, `BreakLoopNullPc`, `IocAlloc`, or `MEM_HISTORY` target dump. `/tmp/mk8d_sigusr1_stall_1780775776.log` captured the post-`NotifyRunning` stall with SIGUSR1: `tid=75` was `RUNNABLE` at `PC=0x01D50438`, which maps via verified `sdk` base `0x01C9C000` to SDK SVC-wrapper offset `0xB4438`, immediately after `svc #0x1C` (`WaitProcessWideKeyAtomic`). The target memory-history ring still contained only `QueryMemory` and `SetHeapSize` entries for `0x068B0000`, no IPC/device mapping.
- Latest CV classification: `/tmp/mk8d_cv554bf2d4_1780775975.log` traced `RUZU_TRACE_CV_KEY=0x554BF2D4` and showed repeated short `wait_insert` events from `tid=75` plus regular `signal_enter` events from `tid=83`. This is not a simple lost wakeup on that CV; the current reproduced symptom is an active scheduler/CV loop after `NotifyRunning`, separate from the original late `nvmap::IocAlloc(0x068B0000)` failure.
- Latest audit cleanup: stale `DIFF.md` claims that nvmap still lacks Host1x-backed SMMU/GMMU integration were removed or narrowed after re-reading upstream `device_memory_manager.inc`; `MaxwellDeviceMemoryManager` comments now describe the mark-region callback as test-only because ASID-backed runtime mappings call the registered `Memory` owner directly. This was documentation/audit cleanup only; no MK8D runtime rerun was needed.
- Latest NvMap audit cleanup: the older 2026-04-04 `PinHandle` missing-item claim about direct concrete Host1x owner references was removed as stale/duplicative. The newer 2026-06-05 `NvMap` entry now tracks only live parity gaps rather than repeating stale owner-shape debt.
- Latest NvMap `Container& core` audit: after re-reading upstream `NvMap::PinHandle`, `NvMap::UnmapHandle`, and `ContainerImpl`, the `Container& core` gap was reclassified from unintentional debt to an intentional safe Rust adaptation. `ContainerSessionStore` carries the same session mapper/ASID state used by those NvMap methods without introducing a `Container -> NvMap -> Container` self-reference cycle.
- Latest NvMap Host1x bridge audit: after re-reading upstream `NvMap::PinHandle` / `UnmapHandle` and the Rust `Host1xCoreInterface` / `video_core::host1x::Host1x` implementation, the concrete `Tegra::Host1x::Host1x&` gap in this NvMap slice was reclassified from missing work to an intentional core/video-core crate-boundary adaptation. The bridge exposes the same owner operations used by upstream for this path: SMMU allocate/map/unmap/free, low-area GMMU map/unmap, and ASID/process registration, while `NvMap` keeps the lifecycle decision ownership.
- Latest `MemoryManager` cache parity progress: after re-reading upstream `video_core/memory_manager.{h,cpp}` and `invalidation_accumulator.h`, Rust `MemoryManager` now owns `InvalidationAccumulator`, `WriteBlockCached` records written GPU ranges, and `FlushCaching` drains them through `get_submapped_device_ranges` into `RasterizerInterface::inner_invalidation`. This also makes the existing `Maxwell3D::process_cb_multi_data` `write_block_cached` call carry the upstream cache-invalidation side effect. Focused tests: `cargo test -p video_core write_block_cached_accumulates_and_flushes_device_ranges -- --nocapture` and `cargo test -p video_core test_process_cb_multi_data_writes_through_memory_manager -- --nocapture`.
- Latest `GetSpan` parity progress: after re-reading upstream `video_core/memory_manager.cpp` and `core/device_memory_manager.inc`, Rust `MemoryManager::get_span` now uses the upstream order `IsContinuousRange -> GpuToCpuAddress -> DeviceMemoryManager::GetSpan`, and `MaxwellDeviceMemoryManager::get_span` applies the upstream continuity check against ruzu's dense device-page SMMU continuity metadata. Focused tests: `cargo test -p video_core get_span_requires_recorded_continuity -- --nocapture` and `cargo test -p video_core get_span_translates_gpu_range_through_device_memory -- --nocapture`.
- Latest dense reverse smoke: after replacing sparse reverse metadata with a dense `compressed_device_addr` table, first MK8D 90s run `/tmp/mk8d_dense_reverse_1780780272.log` hit the known early sparse-log stall before `NotifyRunning`; retry `/tmp/mk8d_dense_reverse_retry_1780780385.log` reached `am::NotifyRunning` with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `glTextureSubImage3D`, or GL API error marker before timeout.
- Latest `physical_base` parity progress: after re-reading upstream `DeviceMemory::{GetRawPhysicalAddr,GetPointerFromRaw}` and `DeviceMemoryManager<Traits>::physical_base/Map/GetPointer/ApplyOpOnPointer`, ASID-backed Host1x SMMU mappings now derive compressed physical pages from the registered process `DeviceMemory` backing base instead of synthetic host-page IDs. Focused test: `cargo test -p video_core gpu_device_memory_manager -- --nocapture` includes `smmu_physical_base_path_uses_raw_physical_page_indices`.
- Latest `ApplyOpOnPointer` parity progress: after re-reading upstream `DeviceMemoryManager<Traits>::ApplyOpOnPAddr`, `ApplyOpOnPointer`, and `TrackContinuityImpl`, registered physical pages with no device alias now still invoke the callback once with `DAddr = subbits`, matching upstream's `base=0` single-entry path. `smmu_track_continuity` also follows upstream null-pointer counting exactly. Focused tests: `cargo test -p video_core smmu_apply_op_on_host_pointer -- --nocapture` and `cargo test -p video_core smmu_track_continuity_after_page_split_mapping_matches_full_range_map -- --nocapture`.
- Latest `physical_base` smoke: `/tmp/mk8d_physical_base_1780780973.log` reached `am::NotifyRunning` under `RUST_LOG=warn`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, or GL API marker was logged before the controlled timeout. This is no-obvious-regression smoke only; it does not prove audio start or rendering progress.
- Latest NvMap runtime fallback smoke: `/tmp/mk8d_nvmap_no_runtime_direct_smmu_1780781397.log` reached `am::NotifyRunning` under `RUST_LOG=warn`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, `active session ... no registered SMMU ASID`, or `runtime SMMU map missing process Memory owner` marker was logged before the controlled timeout. This is no-obvious-regression smoke only; it does not prove audio start or rendering progress.
- Latest NvMap direct-fallback removal smoke: `/tmp/mk8d_nvmap_no_direct_fallback_1780783153.log` reached `am::NotifyRunning` under `RUST_LOG=warn`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, GL API marker, `runtime SMMU map missing Container session store`, `active session ... no registered SMMU ASID`, or `runtime SMMU map missing process Memory owner` marker was logged. The wrapper lost the command exit code after the run because `status` is a read-only zsh variable, so this is no-obvious-regression smoke only.
- Latest `PTEKind` raw-value smoke: `/tmp/mk8d_ptekind_raw_1780781906.log` reached `am::NotifyRunning` under `RUST_LOG=warn`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, `active session ... no registered SMMU ASID`, or `runtime SMMU map missing process Memory owner` marker was logged before the controlled timeout. This is no-obvious-regression smoke only; it does not prove audio start or rendering progress.
- Latest `CopyBlock` parity progress: after re-reading upstream `video_core/memory_manager.cpp`, Rust `MemoryManager::copy_block` now preserves the upstream observable ordering by reading the source GPU VA, writing the destination GPU VA, then flushing the destination range. Focused test: `cargo test -p video_core copy_block_flushes_destination_device_range -- --nocapture`.
- Latest `ReadBlock` parity progress: after re-reading upstream `video_core/memory_manager.{h,cpp}` and `core/device_memory_manager.inc`, Rust `MemoryManager::read_block` now flushes each mapped device segment before reading through the owner-backed device-memory path, while `read_block_unsafe` remains no-flush. Focused test: `cargo test -p video_core read_block_flushes_but_read_block_unsafe_does_not -- --nocapture`.
- Latest unsafe write parity progress: after re-reading upstream `video_core/memory_manager.cpp` and `core/device_memory_manager.inc`, Rust `MaxwellDeviceMemoryManager::smmu_write_block_unsafe` now writes mapped device bytes without invoking the invalidate callback, and `MemoryManager::write_block_unsafe` uses it for GPU query/result writebacks. Focused tests: `cargo test -p video_core write_block_unsafe_does_not_invalidate_range -- --nocapture` and `cargo test -p video_core write_block_unsafe_uses_device_memory_without_invalidation -- --nocapture`.
- Latest `WalkBlock` continuity parity progress: after re-reading upstream `DeviceMemoryManager<Traits>::TrackContinuityImpl`, `GetSpan`, and `WalkBlock`, Rust `MaxwellDeviceMemoryManager::smmu_walk_block` now trusts `continuity_tracker` exactly like upstream instead of revalidating contiguous host pointers page by page. Focused test: `cargo test -p video_core smmu_walk_block_trusts_recorded_continuity_like_upstream -- --nocapture`.
- Latest big-page continuity parity progress: after re-reading upstream `video_core/memory_manager.cpp::BigPageTableOp`, Rust `GpuMemoryManager` now carries the same `MaxwellDeviceMemoryManager` owner as the outer `MemoryManager` and computes `big_page_continuous` by checking every 4 KiB sub-page through `device_memory.get_pointer(...)`, instead of assuming every big-page map is continuous. Focused test: `cargo test -p video_core big_page_continuity_uses_device_memory_pointers -- --nocapture`.
- Latest big-page continuity smoke: `/tmp/mk8d_bigpage_continuity_1780783738.log` reached `am::NotifyRunning` under `RUST_LOG=warn`; no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, `runtime SMMU map missing Container session store`, `active session ... no registered SMMU ASID`, or `runtime SMMU map missing process Memory owner` marker was logged before the controlled timeout. This is no-obvious-regression smoke only; it does not prove audio start or rendering progress.
- Latest `MemoryManager` constructor owner progress: after re-reading upstream `video_core/memory_manager.{h,cpp}`, `control/channel_state.cpp`, and engine constructors in `kepler_compute`, `kepler_memory`, `maxwell_dma`, `fermi_2d`, and `sw_blitter`, legacy `GpuContext::new` constructs Maxwell3D, KeplerCompute, Fermi2D, and MaxwellDMA with one shared owner-backed `MemoryManager`, and its legacy inner `GpuMemoryManager` field is also built with the same `MaxwellDeviceMemoryManager` owner. This matches upstream `ChannelState::Init`'s shared `*memory_manager` owner direction more closely. Ownerless engine `Default` constructors are now `#[cfg(test)]`. Production engine construction must pass an explicit `MemoryManager` owner like upstream; remaining ownerless uses are test-only fixtures.
- Latest `MemoryManager` ownerless API tightening: after re-reading upstream `video_core/memory_manager.{h,cpp}` and `video_core/engines/puller.{h,cpp}`, `MemoryManager::new`, `MemoryManager::new_with_geometry`, `Default for MemoryManager`, `GpuMemoryManager::new`, `GpuMemoryManager::with_params`, `Default for GpuMemoryManager`, and `Default for Puller` are test-only. Production checks prove no normal build path can construct the outer GPU `MemoryManager` or inner `GpuMemoryManager` without a device-memory owner, and both structs now store that owner as a non-optional `Arc<MaxwellDeviceMemoryManager>`. Checks: `cargo check -p video_core`, `cargo check -p video_core --tests`, `cargo check --bin ruzu-cmd`, `cargo fmt --check`, `git diff --check`.
- Latest ASID-backed physical strictness smoke: `/tmp/mk8d_asid_physical_strict_1780785028.log` reached `am::NotifyRunning` under `RUST_LOG=warn` and timed out at 120s with no `0x068B0000`, `BreakLoopNullPc`, panic, SIGSEGV, `IocAlloc`, `glTextureSubImage3D`, GL API marker, `MapSharedMemory`, missing Container session store, missing registered SMMU ASID, or missing process Memory owner marker. This is no-obvious-regression smoke only; it does not prove audio start or rendering progress.
- Latest evidence (`/tmp/mk8d_hle_ipc_memory_authority_1780768098.log` and `/tmp/mk8d_trace_map_68b_1780768235.log`): `HLERequestContext` no longer prefers legacy `process_memory` for IPC input when a `Memory` bridge exists, but MK8D still reaches the same blocker. The IPC descriptor buffer itself is readable through `Memory`; `0x068B0000` is the `IocAllocParams.address` CPU VA requested by the guest, and `KPageTableBase::query_info(0x068B0000)` reports it as `Free`.
- Practical conclusion: continuing only inside `NvMapDevice::IocAlloc` is low value. The next high-value slice is the guest-visible memory-state path that lets MK8D choose or submit an unmapped CPU VA for nvmap, including `QueryMemory`, `SetHeapSize`, `MapPhysicalMemory`, process-region placement, and page-table/kernel-memory parity.
- Earlier MK8D nvmap narrowing around handle `0x258`: ruzu proved `NvMapDevice::IocCreate` receives `size=0x39000` directly from guest code. The missing zuyu `size=0x21000` allocation is skipped before the nvmap ioctl path, not transformed inside `NvMapDevice`.
- Verified MK8D `subsdk0` base: `0x01512000` in `bases.json`. Runtime returns `0x0151FC2C` / `0x0151FC94` map to the internal `subsdk0` resource-size path around `0x0151FAA4`.
- Most useful logs:
- `/tmp/mk8d_nvmap_frame_calc_1780764106.log`: first divergent sequence `0x24C:0x85000`, `0x250:0x7000`, `0x254:0x2000`, `0x258:0x39000`.
- `/tmp/mk8d_nvmap_ptr_h258_1780764512.log`: targeted `0x258` stack/pointer dump; shows guest `FRES` resource data at `0x5C174000`.
- Secondary direction: trace the guest resource-load path immediately before `0x0151FAA4` / `0x01522260`, or compare the corresponding FRES/resource iteration with zuyu. This may explain the earlier `0x258` allocation-size divergence, but it is not the strongest lead for the current `0x068B0000` Null PC blocker.
- Latest page-group/memory-manager parity progress: after re-reading upstream `k_memory_manager.{h,cpp}` and `k_page_group.{h,cpp}`, Rust `KMemoryManager::{open_first,open,close}` now split public ranges per physical `Impl` and lock the owning pool per segment, `KPageGroup::open()` routes to `KMemoryManager::open()`, and `KPageGroup::open_first()` routes to `KMemoryManager::open_first()`. `cargo check -p core --tests` passes, and the focused regression `cargo test -p core open_first_open_close_split_across_impl_managers -- --nocapture` now runs and passes.
- Latest optimized-memory state parity progress: after re-reading upstream `KMemoryManager::InitializeOptimizedMemory` / `FinalizeOptimizedMemory`, Rust now enforces one optimized process per pool, returns `RESULT_BUSY` on a second initializer, stores the optimized process id, and only clears the flag when the same process finalizes. The `Impl` optimized allocation bitmap management remains a documented simplified path. `cargo check -p core --tests` now passes after the core test-harness cleanup.
- Latest `UnmapMemory` rollback parity progress: after re-reading upstream `KPageTableBase::UnmapMemory` and `RemapPageGroup`, Rust now has a private `remap_page_group(...)` helper and uses it to remap the destination alias if source permission restoration fails during `unmap_memory`. The remaining local `MapMemory`/`UnmapMemory` DIFF item is allocator exactness (`num_src_allocator_blocks` / `num_dst_allocator_blocks`), not the rollback remap path.
- Latest `MapMemory`/`UnmapMemory` allocator parity progress: after re-reading upstream allocator construction, Rust now threads the exact `num_src_allocator_blocks` and `num_dst_allocator_blocks` values returned by `check_memory_state_range` into `make_block_update_allocator(...)` for both source and destination updates. The audited `MapMemory`/`UnmapMemory` memory-block update/rollback slice now has no remaining local `Unintentional differences` entry in `DIFF.md`.
- Latest `MapPhysicalMemory` page-group parity progress: after re-reading upstream `KPageTableBase::MapPhysicalMemory`, `Operate(..., KPageGroup, OperationType::MapFirstGroupPhysical)`, and `KMemoryManager::AllocateForProcess`, Rust now allocates physical pages for this path without opening references, passes the owning process id into `allocate_for_process(...)`, clears the fresh physical backing, builds a `KPageGroup cur_pg` per free subrange, and maps via `operate_with_group(..., OperationType::MapFirstGroupPhysical)` instead of direct `OperationType::Map` slicing. `Operate(Unmap/UnmapPhysical)` now closes the page group it unmaps, matching upstream's `MakePageGroup(...)` + `CloseAndReset()` lifetime policy. Checks: `cargo fmt --check`, `cargo check -p core`.
- Latest single-physical-map ref lifecycle progress: after re-reading upstream `KPageTableBase::Operate(..., OperationType::Map)`, `AllocateAndMapPagesImpl`, and `SetupForIpcServer`, Rust single-phys `operate(Map)` now opens heap physical page refs, while `MapPages` and IPC partial-page paths close their temporary `AllocateAndOpenContinuous(...)` allocation refs after the map owns them. The stale DIFF debt about IPC partial pages keeping temporary allocation refs was removed. Checks: `cargo fmt --check`, `git diff --check`, `cargo check -p core`.
- Latest `KPageGroup` constructor parity progress: after re-reading upstream `KPageGroup(KernelCore&, KBlockInfoManager*)`, Rust `KPageGroup::new()` now creates a kernel-owned group by default instead of a detached no-op ref-count group. This makes `KScopedPageGroup` open/close refs for `KPageGroup::new()` callsites such as `MapMemory` / `UnmapMemory`, matching upstream more closely while the remaining `KernelCore&` storage gap stays documented. Checks: `cargo fmt --check`, `git diff --check`, `cargo check -p core`.
- Latest IPC client setup ordering parity progress: after re-reading upstream `KPageTableBase::SetupForIpcClient`, `SetupForIpc`, and `CleanupForIpcClientOnServerSetupFailure`, Rust now changes client-side IPC permissions during client setup, rolls them back with the dedicated failure cleanup if server setup fails, and applies `LockForIpc` only after `SetupForIpcServer` succeeds. The stale DIFF missing item for literal `CleanupForIpcClientOnServerSetupFailure(...)` was removed. Checks: `cargo fmt --check`, `git diff --check`, `cargo check -p core`, `cargo test -p core setup_for_ipc_client_changes_permissions_without_locking_until_server_success -- --nocapture`.
- Latest `UnmapMemory` destination-permission audit: after re-reading upstream `KPageTableBase::UnmapMemory`, the DIFF claim that Rust failed to use upstream `dst_perm` was verified stale. Upstream captures `dst_perm` from `CheckMemoryState(...)` for validation output parity but does not use it afterward; Rust already captures `_out_dst_perm` and applies the same destination unmap/none transition. The stale DIFF item was removed.
- Latest `MapPhysicalMemory` cleanup-shape audit: after re-reading upstream `KPageTableBase::MapPhysicalMemory` / `UnmapPhysicalMemory`, the remaining DIFF missing item asking for literal `SCOPE_GUARD` / `ON_RESULT_FAILURE` macro shape was verified as non-actionable Rust-form debt. The current explicit cleanup helpers carry the same unmap/release ownership effect, so the duplicate missing item was removed while keeping the intentional Rust cleanup-shape adaptation documented.
- Latest `Puller` memory-owner parity progress: after re-reading upstream `video_core/engines/puller.{h,cpp}` and `video_core/memory_manager.{h,cpp}`, Rust `Puller` semaphore reads/writes now use the stored `MemoryManager` owner through `read_block_unsafe` / `write_block_unsafe` instead of passing `Gpu::read_guest_memory` / `Gpu::write_guest_memory` callbacks. This reduces the remaining callback-backed `MemoryManager` debt while preserving the unsafe no-flush/no-invalidate behavior. Focused test added: `cargo test -p video_core read_block_unsafe_uses_device_memory_without_flush -- --nocapture`.
- Latest `DmaPusher` command-fetch owner progress: after re-reading upstream `video_core/dma_pusher.{h,cpp}`, Rust `DmaPusher::fetch_command_bytes` now reads command buffers through `MemoryManager::{read_block,read_block_unsafe}` instead of constructing a CPU guest-memory callback from `Gpu::read_guest_memory`. This matches upstream `GpuGuestMemory(..., memory_manager, ...)` command-buffer fetch ownership while preserving the existing safe/unsafe GPU accuracy decision.
- Latest `KeplerCompute` TIC/TSC owner progress: after re-reading upstream `video_core/engines/kepler_compute.{h,cpp}`, Rust `KeplerCompute::get_tic_entry` / `get_tsc_entry` now read descriptor pools through the stored `MemoryManager` owner with `read_block_unsafe(...)`, matching upstream `memory_manager.ReadBlockUnsafe(...)` and removing the CPU-reader callback from this engine path. Focused tests: `cargo test -p video_core get_tic_entry_reads_from_tic_pool -- --nocapture`, `cargo test -p video_core get_tsc_entry_reads_from_tsc_pool -- --nocapture`.
- Latest `Maxwell3D` TIC/TSC owner progress: after re-reading upstream `video_core/engines/maxwell_3d.{h,cpp}`, Rust `Maxwell3D::get_tic_entry` / `get_tsc_entry` now build their descriptor reader from the stored `MemoryManager` owner with `read_block_unsafe(...)`, matching upstream `memory_manager.ReadBlockUnsafe(...)` and removing the CPU-reader callback from this engine path. Focused tests: `cargo test -p video_core test_get_tic_entry -- --nocapture`, `cargo test -p video_core test_get_tsc_entry -- --nocapture`.
- Latest compute shader environment owner progress: after re-reading upstream `video_core/shader_environment.{h,cpp}`, Rust `ShaderCache::compute_shader` and `ComputeEnvironment::from_kepler_compute` now require only the channel `MemoryManager` owner, not `Maxwell3D::guest_memory_reader`, matching upstream `ComputeEnvironment(..., Tegra::MemoryManager& ...)`.
- Latest Host1x SMMU continuity progress: after re-reading upstream `core/device_memory_manager.inc::DeviceMemoryManager::Map` and `TrackContinuityImpl`, Rust page-split bridge callers (`HeapMapper` and non-preallocated `NvMap::pin_handle`) now install per-page CPU-backed mappings with `track=false` and call `smmu_track_continuity(...)` once for the whole range, matching upstream's map-then-track ordering and restoring multi-page continuity counts. Focused test: `cargo test -p video_core smmu_track_continuity_after_page_split_mapping_matches_full_range_map -- --nocapture`.
- Latest Host1x SMMU page-count progress: after re-reading upstream `core/device_memory_manager.inc::DeviceMemoryManager::{Map,Unmap,TrackContinuityImpl}`, Rust `MaxwellDeviceMemoryManager` now uses upstream `AlignUp(size, page_size)` page counts from `start_page_d` for map, unmap, and continuity coverage instead of including the device-address page offset in the end-page calculation. Focused test: `cargo test -p video_core smmu_unmap_uses_upstream_size_only_page_count_for_unaligned_device_address -- --nocapture`. Broader validation: `cargo test -p video_core gpu_device_memory_manager -- --nocapture`, `cargo check -p video_core`, `cargo fmt --check`, `git diff --check -- video_core/src/host1x/gpu_device_memory_manager.rs DIFF.md`, `cargo build --release --bin ruzu-cmd`, and MK8D 120s smoke `/tmp/mk8d_smmu_size_pages_1780814355.log` reached `am::NotifyRunning` with no targeted SMMU/nvmap/null-PC fatal markers before controlled timeout.
- Latest Host1xCoreInterface cleanup: after re-reading upstream `HeapMapper::Map` and `NvMap::PinHandle`, the core-facing Host1x trait no longer exposes `smmu_map_with_cpu_backing(...)`. Runtime `HeapMapper` and `NvMap` can only request ASID-backed `smmu_map(...)`, matching upstream `DeviceMemoryManager::Map(address, virtual_address, size, asid, track)`. The host-pointer helper remains local to `MaxwellDeviceMemoryManager` for reduced video_core fixtures. Checks: `cargo check -p core`, `cargo check -p video_core`.
- Latest transfer-memory page-group smoke: `/tmp/mk8d_transfer_memory_1780806629.log` reached `IAudioRendererManager::OpenAudioRenderer`, `IAudioRenderer::Start`, and `am::NotifyRunning`, then timed out cleanly at 120s. No `0x068B0000`, `D401`, `BreakLoopNullPc`, panic, SIGSEGV, or `KMemoryManager: no manager for address` marker was logged. This validates the immediate transfer-memory finalize crash is gone in this smoke, but it does not prove the broader MK8D rendering/audio progression is solved.

## PREVIOUS: fix-diff DIFF.md cleanup

Create or continue the `fix-diff` branch and process every `Unintentional differences (to fix)` and `Missing items` entry in `DIFF.md`.

For each item:

- First verify whether the item is still true by checking the matching upstream source under `/home/vricosti/Dev/emulators/zuyu/src/`.
- If the item is still valid, fix it in the Rust port while preserving upstream ownership, structure, constants, ordering, and behavior as closely as possible.
- If the item is stale or invalid, remove it from `DIFF.md`.
- Once an item is fixed, remove it from `DIFF.md`.
- Do not add new `DIFF.md` entries merely saying an item was fixed.

Completion requires all `Unintentional differences (to fix)` and `Missing items` entries in `DIFF.md` to be either fixed and removed, or verified stale and removed.

Status: active, not complete.
